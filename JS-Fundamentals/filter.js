/* filter() is an array method that returns a new array with all the element that passes
the test */

const nums = [1, 2, 3, 4, 5, 6, 7, 8, 9];
const result = nums.filter((x) => x % 2 === 0); // returns numbers divisible by 2
console.log(nums);
// console.log(result);
